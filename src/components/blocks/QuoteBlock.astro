---
const { data } = Astro.props;
---

<section class="py-12 px-6">
  <div class="container mx-auto max-w-3xl">
    <figure class="border-l-4 border-emerald-800 pl-6 py-2">
      <blockquote class="font-semibold text-avocado">
        <p class="text-2xl font-serif">
          <Fragment set:html={data.quote} />
        </p>
      </blockquote>
      {data.attribution && (
        <figcaption class="mt-2 text-gray-600">— {data.attribution}</figcaption>
      )}
    </figure>
  </div>
</section>
